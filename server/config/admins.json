{
  "_comment": "Admin credentials — the single source of truth for /api/auth/admin/login. Plaintext on purpose: this file lives in a private repo. Edit + redeploy to add or remove admins. Each entry needs `username` + `password`; `name` is shown in the greeting on the admin dashboard. NEVER expose this directory via static hosting.",
  "_example_entries": [
    { "username": "alex",  "password": "<plaintext>", "name": "Alex Morgan" },
    { "username": "sarah", "password": "<plaintext>", "name": "Sarah Chen" }
  ],
  "admins": [
    {
      "username": "admin",
      "password": "admin",
      "name": "Admin"
    }
  ]
}
